---
import { isChinaLocale } from "../i18n/china-translations";
import { localePath } from "../i18n/config";
import type { Locale } from "../i18n/config";
import { business } from "../data/business";

interface Props {
  locale: Locale;
  whatsappLabel: string;
  wechatLabel: string;
  class?: string;
  /** Optional WhatsApp message prefill (ignored for WeChat locales). */
  whatsappPrefill?: string;
}

const {
  locale,
  whatsappLabel,
  wechatLabel,
  class: className = "button button-primary",
  whatsappPrefill,
} = Astro.props;

const whatsappHref = whatsappPrefill
  ? `${business.whatsappUrl}?text=${encodeURIComponent(whatsappPrefill)}`
  : business.whatsappUrl;
---

{
  isChinaLocale(locale) ? (
    <a
      class={className}
      href={`${localePath(locale, "contact")}#wechat`}
      data-analytics-event="wechat_click"
      data-analytics-label="cta_button"
    >
      {wechatLabel}
    </a>
  ) : (
    <a
      class={className}
      href={whatsappHref}
      target="_blank"
      rel="noopener noreferrer"
      data-analytics-event="whatsapp_click"
      data-analytics-label="cta_button"
    >
      {whatsappLabel}
    </a>
  )
}
